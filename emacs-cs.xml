<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<?xml-stylesheet type="text/xsl" href="tohtml.xsl"?>

<cheatsheet version="1.0" lang="en" created="2017-09-29">

  <header>
    <title>EMACS Cheatsheet</title>
    <subtitle>For version 25</subtitle>
  </header>

  <body>
    <chapter title="Command line options">
      <it>
        <k>--visit=&lt;filespec&gt; | --file=&lt;filespec&gt; |
        &lt;filespec&gt;</k>
        <f/>
        <d>Open filespec into individual buffers for editing.</d>
      </it>

      <it>
        <k>+row[:column]</k>
        <f/>
        <d>Move point to line number row and (optional) horizontal
        position column in the file (default is +1:1).</d>
      </it>

      <it>
        <k>--insert &lt;file&gt;</k>
        <f/>
        <d>Insert file at the beginning of the buffer.</d>
      </it>

      <it>
        <k>--load &lt;file&gt; | -l &lt;file&gt;</k>
        <f/>
        <d>Execute the Emacs Lisp instructions in file.</d>
      </it>

      <it>
        <k>--g &lt;dimensions&gt; | --geometry &lt;dimensions&gt;</k>
        <f/>
        <d>Set the window's width, height, and position according to
        the given X window dimensions (the default is to make the
        window 80x40 characters).</d>
      </it>

      <it>
        <k>-nw | --no-windows</k>
        <f/>
        <d>In X, don't use an X client window, but open in the current
        terminal window instead. This option doesn't affect console
        sessions.</d>
      </it>
    </chapter>

    <chapter title="Prefixes">
      <it>
        <k>C-c</k>
        <f/>
        <d>Commands particular to the current editing mode</d>
      </it>

      <it>
        <k>C-x</k>
        <f/>
        <d>Commands for files and buffers</d>
      </it>

      <it>
        <k>C-h</k>
        <f/>
        <d>Help commands</d>
      </it>

      <it>
        <k>M-x</k>
        <f/>
        <d>Literal function name</d>
      </it>

      <it>
        <k>M-!</k>
        <f>shell-command</f>
        <d>Execute external shell command from within Emacs.The output
        from the shell command is displayed in the minibuffer or in a
        separate buffer, depending on the output size. When used with
        a prefix argument (e.g, <cbd>C-u M-!</cbd>), the shell-command
        output is inserted in the current buffer at point.</d>
      </it>

      <it>
        <k>M-|</k>
        <f>shell-command-on-region</f>
        <d>Provide the region text to the shell command as input. If
        you want the shell to replace the region text with the output
        from the shell command, use <kbd>C-u M-|</kbd>.</d>
      </it>
    </chapter>

    <chapter title="Buffer and files functions">
      <it>
        <k>C-x C-f</k>
        <f>find-file</f>
        <d>Visit a file.</d>
      </it>
      <it>
        <k>C-x C-s</k>
        <f>save-buffer</f>
        <d>Save current buffer to disk.</d>
      </it>

      <it>
        <k>C-x s</k>
        <f>save-some-buffers</f>
        <d>Ask about saving all unsaved buffers to disk.</d>
      </it>

      <it>
        <k>C-x C-c</k>
        <f>save-buffers-kill-emacs</f>
        <d>Ask about saving all unsaved buffers to disk and exit
        Emacs.</d>
      </it>

      <it>
        <k>C-x C-z | C-z</k>
        <f>suspend-emacs</f>
        <d>Suspend Emacs and make it a background process (press
        <kbd>fg | fg %emacs</kbd> to <i>awake</i> it back.</d>
      </it>

      <it>
        <k>C-x C-b</k>
        <f>list-buffers</f>
        <d>List all buffers.</d>
      </it>

      <it>
        <k>C-x k</k>
        <f>kill-buffer</f>
        <d>Kill a buffer (the current buffer, by default).</d>
      </it>

      <it>
        <k>C-x C-q</k>
        <f>vc-toggle-read-only</f>
        <d>Toggle read-only status on the current buffer (and perform
        version control if applicable).</d>
      </it>

      <it>
        <k>C-x i</k>
        <f>insert-file</f>
        <d>Insert the contents of a file at point.</d>
      </it>
    </chapter>

    <chapter title="Movement and navigation">
      <it>
        <k>C-p | UpArrow</k>
        <f>previous-line</f>
        <d>Move point up to the previous line.</d>
      </it>

      <it>
        <k>C-n | DownArrow</k>
        <f>next-line</f>
        <d>Move point down to the next line.</d>
      </it>

      <it>
        <k>C-f | RightArrow</k>
        <f>forward-char</f>
        <d>Move point forward to the next character.</d>
      </it>

      <it>
        <k>C-b | LeftArrow</k>
        <f>back-char</f>
        <d>Move point backward to the previous character.</d>
      </it>

      <it>
        <k>M-f | C-RightArrow</k>
        <f>forward-word</f>
        <d>Move point forward to the next word.</d>
      </it>

      <it>
        <k>M-b | C-LeftArrow</k>
        <f>backward-word</f>
        <d>Move point backward to the previous word.</d>
      </it>

      <it>
        <k>C-v | PgDn</k>
        <f>scroll-up</f>
        <d>Scroll the text upward by a screen.</d>
      </it>

      <it>
        <k>M-v | PgUp</k>
        <f>scroll-down</f>
        <d>Scroll the text downward by a screen.</d>
      </it>

      <it>
        <k>C-Home</k>
        <f>beginning-of-buffer</f>
        <d>Move point to the beginning of the buffer. (On some
        versions, this key is defined by default to move to the
        beginning of the current line.)</d>
      </it>

      <it>
        <k>C-End</k>
        <f>end-of-buffer</f>
        <d>Move point to the end of the buffer. (On some versions,
        this key is defined by default to move to the end of the
        current line.)</d>
      </it>

      <it>
        <k>Home | C-a</k>
        <f>beginning-of-line</f>
        <d>Move point to the beginning of the line.</d>
      </it>

      <it>
        <k>End | C-e</k>
        <f>end-of-line</f>
        <d>Move point to the end of the line.</d>
      </it>

      <it>
        <k>M-a</k>
        <f>beginning-of-sentence</f>
        <d>Move point to the beginning of the sentence.</d>
      </it>

      <it>
        <k>M-e</k>
        <f>end-of-sentence</f>
        <d>Move point to the end of the sentence.</d>
      </it>

      <it>
        <k>C-{</k>
        <f>beginning-of-paragraph</f>
        <d>Move point to the beginning of the paragraph.</d>
      </it>

      <it>
        <k>C-}</k>
        <f>end-of-paragraph</f>
        <d>Move point to the end of the paragraph.</d>
      </it>
    </chapter>

    <chapter title="Navigating over balanced expressions">
      <it>
        <k>C-M-n</k>
        <f>forward-list</f>
        <d>Move forward over a parenthetical group.</d>
      </it>

      <it>
        <k>C-M-p</k>
        <f>backward-list</f>
        <d>Move backward over a parenthetical group.</d>
      </it>

      <it>
        <k>C-M-f</k>
        <f>forward-sexp</f>
        <d>Move forward over a balanced expression.</d>
      </it>

      <it>
        <k>C-M-b</k>
        <f>backward-sexp</f>
        <d>Move backward over a balanced expression.</d>
      </it>

      <it>
        <k>C-M-k</k>
        <f>kill-sexp</f>
        <d>Kill balanced expression forward.</d>
      </it>

      <it>
        <k>C-M-SPC</k>
        <f>mark-sexp</f>
        <d>Put the mark at the end of the sexp.</d>
      </it>
    </chapter>

    <chapter title="Common editing commands">
      <it>
        <k>Ins</k>
        <f>overwrite-mode</f>
        <d>Toggle overwrite mode (default is off).</d>
      </it>

      <it>
        <k>Backspace | Del</k>
        <f>delete-backward-char</f>
        <d>Delete the character before point.</d>
      </it>

      <it>
        <k>C-d</k>
        <f>delete-char</f>
        <d>Delete the character at point.</d>
      </it>

      <it>
        <k>M-d</k>
        <f>kill-word</f>
        <d>Delete the characters from point forward to the end of the
        word.</d>
      </it>

      <it>
        <k>M-Backspace | M-Del</k>
        <f>backward-kill-word</f>
        <d>Delete the characters from point backward to the beginning
        of the word.</d>
      </it>

      <it>
        <k>C-_</k>
        <f>undo</f>
        <d>Undo your last typing or action.</d>
      </it>

      <it>
        <k>C-q[CHAR] or [NUM]</k>
        <f>quoted-insert</f>
        <d>Insert, at point, the literal character keypress or the
        character whose octal value is XXX.</d>
      </it>

      <it>
        <k>C-u[NUM][COMMAND]</k>
        <f>universal-argument</f>
        <d>Execute command a total of number (default 4) times in
        succession.</d>
      </it>
    </chapter>

    <chapter title="Functions for marking and killing text">
      <it>
        <k>C-Space</k>
        <f>set-mark-command</f>
        <d>Set the mark at point.</d>
      </it>

      <it>
        <k>C-k</k>
        <f>kill-line</f>
        <d>Kill all text from point to the end of the line.</d>
      </it>

      <it>
        <k>C-w</k>
        <f>kill-region</f>
        <d>Kill the region.</d>
      </it>

      <it>
        <k>M-w</k>
        <f>kill-ring-save</f>
        <d>Save the region in the kill ring, but don't kill it.</d>
      </it>

      <it>
        <k>C-y</k>
        <f>yank</f>
        <d>Yank text from the kill ring.</d>
      </it>
    </chapter>

    <chapter title="Text mode key bindings">
      <it>
        <k>Esc</k>
        <f/>
        <d>Prefix for mode-specific commands</d>
      </it>

      <it>
        <k>Esc Tab | M-Tab</k>
        <f>ispell-complete-word</f>
        <d/>
      </it>

      <it>
        <k>Esc S | M-S</k>
        <f>center-paragraph</f>
        <d/>
      </it>

      <it>
        <k>Esc s | M-s</k>
        <f>center-line</f>
        <d/>
      </it>
    </chapter>

    <chapter title="Text manipulation commands">
      <it>
        <k>C-x C-i | C-x Tab</k>
        <f>indent-rigidly</f>
        <d>This command indents lines in the region (or at point).</d>
      </it>

      <it>
        <k/>
        <f>fill-region</f>
        <d>This command fills all paragraphs in the region.</d>
      </it>

      <it>
        <k>M-q</k>
        <f>fill-paragraph</f>
        <d>This command fills the single paragraph at point.</d>
      </it>

      <it>
        <k>M-\</k>
        <f>delete-horizontal-space</f>
        <d>This command removes any horizontal space to the right and
        left of point.</d>
      </it>

      <it>
        <k>C-o</k>
        <f>open-line</f>
        <d>This command opens a new line of vertical space below
        point, without moving point.</d>
      </it>

      <it>
        <k>C-t</k>
        <f>transpose-chars</f>
        <d>This command transposes the single characters to the right
        and left of point.</d>
      </it>

      <it>
        <k>M-t</k>
        <f>transpose-words</f>
        <d>This command transposes the single words to the right and
        left of point.</d>
      </it>

      <it>
        <k>C-x C-t</k>
        <f>transpose-lines</f>
        <d>This command transposes the line at point with the line
        before it.</d>
      </it>

      <it>
        <k>M-^</k>
        <f>delete-indentation</f>
        <d>This command joins the line at point with the previous
        line. Preface with <kbd>C-1</kbd> to join the line at point
        with the next line.</d>
      </it>

      <it>
        <k>M-u</k>
        <f>uppercase-word</f>
        <d>This command converts the text at point to the end of the
        word to uppercase letters.</d>
      </it>

      <it>
        <k>M-l</k>
        <f>downcase-word</f>
        <d>This command converts the text at point to the end of the
        word to lowercase letters.</d>
      </it>

      <it>
        <k>C-x C-l</k>
        <f>downcase-region</f>
        <d>This command converts the region to lowercase letters.</d>
      </it>

      <it>
        <k>C-x C-u</k>
        <f>upcase-region</f>
        <d>This command converts the region to uppercase letters.</d>
      </it>
    </chapter>

    <chapter title="Search and replace commands">
      <it>
        <k>C-s [STRING] [C-w] [C-y]</k>
        <f>isearch-forward</f>
        <d>Incrementally search forward through the buffer for string
        (default is the last search string you gave, if any);
        <kbd>C-w</kbd> uses the text from point forward to the end of
        the word and <kbd>C-y</kbd> uses everything from point to the
        end of the line.</d>
      </it>

      <it>
        <k>C-r [STRING] [C-w] [C-y]</k>
        <f>isearch-backward</f>
        <d>Incrementally search backward through the buffer for string
        (default is the last search string you gave, if any);
        <kbd>C-w</kbd> uses the text from point forward to the end of
        the word, and <kbd>C-y</kbd> uses everything from point to the
        end of the line.</d>
      </it>

      <it>
        <k>C-s Enter C-w [WORD OR PHRASE]</k>
        <f>word-search-forward</f>
        <d>Search forward through the buffer for the given word or
        phrase, regardless of spacing.</d>
      </it>

      <it>
        <k>C-r Enter C-w [WORD OR PHRASE]</k>
        <f>word-search-backward</f>
        <d>Search backward through the buffer for the given word or
        phrase, regardless of spacing.</d>
      </it>

      <it>
        <k>C-M-s</k>
        <f>isearch-forward-regexp</f>
        <d>Incrementally search forward through the buffer for a given
        regular expression.</d>
      </it>

      <it>
        <k>C-M-r</k>
        <f>isearch-backward-regexp</f>
        <d>Incrementally search backward through the buffer for a
        given regular expression.</d>
      </it>

      <it>
        <k/>
        <f>replace-string</f>
        <d>Search for a given string from point to the end of the
        buffer and replace it with a given string.</d>
      </it>

      <it>
        <k/>
        <f>replace-regexp</f>
        <d>Search for a given regular expression from point to the end
        of the buffer and replace it with a given string.</d>
      </it>

      <it>
        <k>M-%</k>
        <f>query-replace</f>
        <d>Search for a given string from point to the end of the
        buffer and, in each instance, query to replace it with a
        given string.</d>
      </it>

      <it>
        <k>C-M-%</k>
        <f>query-replace-regexp</f>
        <d>Search for a given regular expression from point to the end
        of the buffer and, in each instance, query to replace it
        with a given string.</d>
      </it>
    </chapter>

    <chapter title="Commands for using rectangles">
      <it>
        <k>C-space</k>
        <f>set-mark-command</f>
        <d>Marks one corner of a rectangle (point marks the opposite
        corner).</d>
      </it>

      <it>
        <k>C-x r k</k>
        <f>kill-rectangle</f>
        <d>Kills the current rectangle and saves it in a special
        rectangle buffer.</d>
      </it>

      <it>
        <k>C-x r d</k>
        <f>delete-rectangle</f>
        <d>Deletes the current rectangle and doesn't save it for
        yanking.</d>
      </it>

      <it>
        <k>C-x r c</k>
        <f>clear-rectangle</f>
        <d>Clears the current rectangle, replacing the entire area
        with whitespace.</d>
      </it>

      <it>
        <k>C-x r o</k>
        <f>open-rectangle</f>
        <d>Opens the current rectangle, filling the entire area with
        whitespace and moving all text from the rectangle to the
        right.</d>
      </it>

      <it>
        <k>C-x r y</k>
        <f>yank-rectangle</f>
        <d>Yanks the contents of the last-killed rectangle at point,
        moving all existing text to the right.</d>
      </it>
    </chapter>

    <chapter title="Advanced mark and selection commands">
      <it>
        <k>C-u C-space</k>
        <f>pop-to-mark-command</f>
        <d>Moves to the previous mark in the mark ring.</d>
      </it>

      <it>
        <k>C-x C-x</k>
        <f>exchange-point-and-mark</f>
        <d>Swaps the location of point and the mark.</d>
      </it>

      <it>
        <k>M-@</k>
        <f>mark-word</f>
        <d>Marks all text from point to the end of the current
        word.</d>
      </it>

      <it>
        <k>M-h</k>
        <f>mark-paragraph</f>
        <d>Marks the current paragraph, regardless of the location of
        point.</d>
      </it>

      <it>
        <k/>
        <f>transient-mark-mode</f>
        <d>Toggles Transient Mark mode.</d>
      </it>

      <it>
        <k>C-x h</k>
        <f>mark-whole-buffer</f>
        <d>Marks the entire buffer, regardless of the location of
        point.</d>
      </it>
    </chapter>

    <chapter title="Advanced kill and yank commands">
      <it>
        <k>[NUM] C-k</k>
        <f>kill-line</f>
        <d>Kills integer number of lines. If 0, kills from point to
        the beginning of the line; if negative, kills in reverse (not
        sure for version 25).</d>
      </it>

      <it>
        <k>M-k</k>
        <f>kill-sentence</f>
        <d>Kills from point to the end of the sentence.</d>
      </it>

      <it>
        <k>M-z</k>
        <f>zap-to-char</f>
        <d>Zaps all text from point to the specified character.</d>
      </it>

      <it>
        <k>M-y</k>
        <f>yank-pop</f>
        <d>Moves to the next slot in the kill ring.</d>
      </it>

      <it>
        <k>[NUM] C-y</k>
        <f>yank</f>
        <d>Yanks the specified slot in the kill ring.</d>
      </it>
    </chapter>

    <chapter title="Registers commands">
      <it>
        <k>C-x r space X</k>
        <f>point-to-register</f>
        <d>Save point to register X.</d>
      </it>

      <it>
        <k>C-x r s X</k>
        <f>copy-to-register</f>
        <d>Save the region to register X.</d>
      </it>

      <it>
        <k>C-x r r X</k>
        <f>copy-rectangle-to-register</f>
        <d>Save the selected rectangle to register X.</d>
      </it>

      <it>
        <k/>
        <f>view-register</f>
        <d>View the contents of a given register.</d>
      </it>

      <it>
        <k>C-x r j X</k>
        <f>jump-to-register</f>
        <d>Move point to the location given in register X.</d>
      </it>

      <it>
        <k>C-x r i X</k>
        <f>insert-register</f>
        <d>Insert the contents of register X at point.</d>
      </it>
    </chapter>

    <chapter title="Bookmarks commands">
      <it>
        <k>C-x r m Bookmark</k>
        <f>bookmark-set</f>
        <d>Set a bookmark named Bookmark.</d>
      </it>

      <it>
        <k>C-x r l</k>
        <f>bookmarks-bmenu-list</f>
        <d>List all saved bookmarks.</d>
      </it>

      <it>
        <k/>
        <f>bookmark-delete</f>
        <d>Delete a bookmark.</d>
      </it>

      <it>
        <k>C-x r b Bookmark</k>
        <f>bookmark-jump</f>
        <d>Jump to the location set in the bookmark named
        Bookmark.</d>
      </it>

      <it>
        <k/>
        <f>bookmark-save</f>
        <d>Save all bookmarks to the bookmark file, ~/.emacs.bmk.</d>
      </it>
    </chapter>

    <chapter title="Window-manipulation commands">
      <it>
        <k>C-x 2</k>
        <f>split-window-vertically</f>
        <d>Split the current window in half across the middle,
        stacking the new buffers vertically.</d>
      </it>

      <it>
        <k>C-x 4 b</k>
        <f>switch-to-buffer-other-window</f>
        <d>Split the current window in half vertically, prompting for
        the buffer to use the bottom window and making that the active
        window.</d>
      </it>

      <it>
        <k>C-x 4 C-o</k>
        <f>display-buffer</f>
        <d>Display a buffer in another window, prompting for the
        buffer to use the other window but keeping the current window
        active. (If only one window exists, then split the window
        vertically to display the other buffer.)</d>
      </it>

      <it>
        <k>C-x 4 f</k>
        <f>find-file-other-window</f>
        <d>Open a new file in a new buffer, drawing it in a new
        vertical window.</d>
      </it>

      <it>
        <k>C-x 4 r</k>
        <f>find-file-read-only-other-window</f>
        <d>Open a new file in a new read-only buffer, drawing it in a
        new vertical window.</d>
      </it>

      <it>
        <k>C-M-v</k>
        <f>scroll-other-window</f>
        <d>Scroll to the window that would be the next one to switch
        to with <kbd>C-x o</kbd>.</d>
      </it>

      <it>
        <k/>
        <f>scroll-all</f>
        <d>Toggle the scroll-all minor mode. When it's on, all windows
        displaying the buffer in the current window are scrolled
        simultaneously and in equal, relative amounts.</d>
      </it>

      <it>
        <k>C-x o</k>
        <f>other-window</f>
        <d>Move the cursor to the next window, and make it the active
        window.</d>
      </it>

      <it>
        <k>C-x 0</k>
        <f>delete-window</f>
        <d>Delete the current window, and move the cursor to the
        window that would be the next one to switch to with
        <kbd>C-x o</kbd>.</d>
      </it>

      <it>
        <k>C-x 1</k>
        <f>delete-other-windows</f>
        <d>Delete all windows except the current window.</d>
      </it>

      <it>
        <k>C-x 4 0</k>
        <f>kill-buffer-and-window</f>
        <d>Delete the current window, and kill its buffer.</d>
      </it>

      <it>
        <k>C-x 3</k>
        <f>split-window-horizontally</f>
        <d>Split the current window in half down the middle, stacking
        the new buffers horizontally.</d>
      </it>

      <it>
        <k/>
        <f>follow-mode</f>
        <d>Toggle follow, a minor mode. When it's on in a buffer, all
        windows displaying the buffer are connected into a large
        virtual window.</d>
      </it>

      <it>
        <k>C-x ^</k>
        <f>enlarge-window</f>
        <d>Make the current window taller by a line; preceded by a
        negative, this makes the current window shorter by a line.</d>
      </it>

      <it>
        <k>C-x }</k>
        <f>shrink-window-horizontally</f>
        <d>Make the current active window thinner by a single
        column.</d>
      </it>

      <it>
        <k>C-x {</k>
        <f>enlarge-window-horizontally</f>
        <d>Make the current active window wider by a single
        column.</d>
      </it>

      <it>
        <k>C-x -</k>
        <f>shrink-window-if-larger-than-buffer</f>
        <d>Reduce the current active window to the smallest possible
        size for the buffer it contains.</d>
      </it>

      <it>
        <k>C-x +</k>
        <f>balance-windows</f>
        <d>Balance the size of all windows, making them approximately
        equal.</d>
      </it>

      <it>
        <k/>
        <f>compare-windows</f>
        <d>Compare the current window with the next window, beginning
        with point in both windows and moving point in both buffers to
        the first character that differs until reaching the end of the
        buffer.</d>
      </it>
    </chapter>

    <chapter title="Shell commands">
      <it>
        <k>M-! &lt;cmd&gt;</k>
        <f>shell-command</f>
        <d>Run the shell command line <kbd>cmd</kbd> and display the
        output.</d>
      </it>

      <it>
        <k>M-| &lt;cmd&gt;</k>
        <f>shell-command-on-region</f>
        <d>Run the shell command line <kbd>cmd</kbd> with region
        contents as input; optionally replace the region with the
        output.</d>
      </it>

      <it>
        <k/>
        <f>shell</f>
        <d>Run a subshell with input and output through an Emacs
        buffer. You can then give commands interactively.</d>
      </it>

      <it>
        <k/>
        <f>term</f>
        <d>Run a subshell with input and output through an Emacs
        buffer. You can then give commands interactively. Full
        terminal emulation is available.</d>
      </it>

      <it>
        <k/>
        <f>eshell</f>
        <d>Start the Emacs shell.</d>
      </it>
    </chapter>

    <chapter title="nXML mode">
      <it>
        <k>C-c C-n</k>
        <f>rng-next-error</f>
        <d>Move to the next location where the document
        structure is not valid. If the document isn't valid, the
        cursor will jump to the probable error, and display a message
        in the minibuffer explaining what it doesn't like. If the
        document is valid from the cursor to the end of the file, the
        message “No more errors” appears in the minibuffer. To
        validate the entire document, move to the top of the document
        with <kbd>M-&lt;</kbd> and then use this key sequence. If it
        says “No more errors” with point at the top of the file, the
        entire file is valid; otherwise it will jump the cursor to the
        next invalid content.</d>
      </it>

      <it>
        <k>C-c C-v</k>
        <f>rng-validate-mode</f>
        <d>Turn validation on or off. If validation is turned on, in
        the status line's mode area you will see either “nXML Valid”
        or “nXML Invalid”. If validation is turned off, neither word
        will appear after “nXML” in the mode line. You may want to
        turn validation off during serious document surgery, then turn
        it back on when you think it's valid again.</d>
      </it>

      <it>
        <k>tab</k>
        <f>indent-for-tab-command</f>
        <d>Indent the current line according to the level of nested
        block tags. The indentation is two spaces per level.</d>
      </it>

      <it>
        <k>M-C-\</k>
        <f>indent-region</f>
        <d>Indent all the lines in the region using the same process
        as for tab.</d>
      </it>

      <it>
        <k>C-c C-f</k>
        <f>nxml-finish-element</f>
        <d>Insert an end tag for whatever element the cursor is
        in. This works whether you are still inside the start tag or
        in the content.</d>
      </it>

      <it>
        <k>C-c C-i</k>
        <f>nxml-balanced-close-start-tag-inline</f>
        <d>Used when you have finished the start tag of an
        inline element, up to but not including the closing
        “&gt;”. This command adds the closing “&gt;” and an end tag,
        and then places the cursor between the tags so you can type
        the content.</d>
      </it>

      <it>
        <k>C-c C-b</k>
        <f>nxml-balanced-close-start-tag-block</f>
        <d>Like <kbd>C-c C-i</kbd>, but used with block elements. The
        command adds the closing “&gt;”, then a blank line, then an
        end tag on yet another separate line. The cursor is left
        indented at the proper level on the central blank line.</d>
      </it>

      <it>
        <k>M-q</k>
        <f>fill-paragraph</f>
        <d>Reformat the paragraph containing the cursor. This works
        best if the content does not start on the same line as the
        start tag.</d>
      </it>

      <it>
        <k>C-c C-x</k>
        <f>nxml-insert-xml-declaration</f>
        <d>Inserts an XML processing instruction at the top of the
        file.</d>
      </it>

      <it>
        <k>M-C-f</k>
        <f>forward-sexp</f>
        <d>Move forward over tag. If point is not inside a
        tag, it moves to a position just before the next tag. If point
        is inside a start tag, it jumps to a position just before the
        closing “&gt;”. If point is inside an end tag, it moves just
        past the end tag.</d>
      </it>

      <it>
        <k>M-C-b</k>
        <f>backward-sexp</f>
        <d>Move backward over tag. If point is not inside a
        tag, it moves just after the previous start tag. If point is
        inside a start tag, it jumps to a position just after the
        starting “&lt;”. If point is inside an end tag, it moves just
        before that tag.</d>
      </it>

      <it>
        <k>M-C-n</k>
        <f>nxml-forward-element</f>
        <d>Move the cursor to the end of the next element.</d>
      </it>

      <it>
        <k>M-C-p</k>
        <f>nxml-backward-element</f>
        <d>Move the cursor before the previous element.</d>
      </it>

      <it>
        <k>M-C-d</k>
        <f>nxml-down-element</f>
        <d>Move the cursor to the next included element after point,
        to a position just after the start tag; d is for “down.”</d>
      </it>

      <it>
        <k>M-C-u</k>
        <f>nxml-backward-up-element</f>
        <d>Move the cursor to a position just before the start tag of
        the element containing point; u is for “up.”</d>
      </it>

      <it>
        <k>C-c C-o C-d</k>
        <f>nxml-hide-subheadings</f>
        <d>Hide the children of the current element, as in emacs
        outline-mode.</d>
      </it>

      <it>
        <k>C-c C-o C-s</k>
        <f>nxml-hide-subheadings</f>
        <d>Reverses the action of <kbd>C-c C-o C-d</kbd>, revealing
        the children of the current element.</d>
      </it>
    </chapter>

    <chapter title="Managing variables">
      <it>
	      <k/>
	      <f>set-variable</f>
	      <d>Ask for a variable to change and for the needed value.</d>
      </it>

      <it>
	      <k/>
	      <f>auto-mode-alist</f>
	      <d>If set to <em>nil</em>, automatic selection of major mode
	      based on file name extension is turned off. Its default value
	      is a list of file name extensions and corresponding modes.</d>
      </it>

      <it>
	      <k/>
	      <f>auto-save-default</f>
	      <d>If not set to <em>nil</em>, Emacs automatically saves a
	      changed buffer to its corresponding file at preset
	      intervals. Its default value is t.</d>
      </it>

      <it>
	      <k/>
	      <f>auto-save-interval</f>
	      <d>Contains the number of character changes after which
	      Auto-save mode, if true, is invoked; the default value is
	      300.</d>
      </it>

      <it>
	      <k/>
	      <f>calendar-latitude</f>
	      <d>Contains the latitude value for the location of the user's
	      workstation, in degrees; the default value is
	      <em>nil</em>.</d>
      </it>

      <it>
	      <k/>
	      <f>calendar-longitude</f>
	      <d>Contains the longitude value for the location of the user's
	      workstation, in degrees; the default value is
	      <em>nil</em>.</d>
      </it>

      <it>
	      <k/>
	      <f>calendar-location-name</f>
	      <d>Contains the value for the location name (such as city,
	      state, and country) for the location of the user's
	      workstation; the default value is <em>nil</em>.</d>
      </it>

      <it>
	      <k/>
	      <f>colon-double-space</f>
	      <d>If not set to <em>nil</em>, commands for filling text
	      insert two spaces after a colon instead of one. The default
	      value is nil.</d>
      </it>

      <it>
	      <k/>
	      <f>command-line-args</f>
	      <d>Contains the list of arguments used in the command line
	      that executed the current Emacs session.</d>
      </it>

      <it>
	      <k/>
	      <f>command-line-default-directory</f>
	      <d>Contains the path name of the directory from which the
	      current Emacs session was executed.</d>
      </it>

      <it>
	      <k/>
	      <f>compare-ignore-case</f>
	      <d>If not set to <em>nil</em>, Emacs ignores differences in
	      uppercase and lowercase letters when running the
	      compare-windows function, as described in fifth installment of
	      this series (see Resources). The default value is
	      <em>nil</em>.</d>
      </it>

      <it>
	      <k/>
	      <f>confirm-kill-emacs</f>
	      <d>If set to <em>nil</em>, Emacs doesn't ask for a
	      confirmation when exiting; otherwise, the exit verification
	      might be customized as an Emacs Lisp function such as
	      <em>y-or-n-p</em>. The default value is <em>nil</em>.</d>
      </it>

      <it>
	      <k/>
	      <f>default-justification</f>
	      <d>Sets the default justification style. The value can be one
	      of left, right, center, full, or none. The default value is
	      left.</d>
      </it>

      <it>
	      <k/>
	      <f>default-major-mode</f>
	      <d>Selects the default major mode for new files or
	      buffers. The default value is <em>fundamental-mode</em>.</d>
      </it>

      <it>
	      <k/>
	      <f>display-time-24hr-format</f>
	      <d>If set to t, Emacs displays time in 24-hour military
	      format, instead of the standard 12-hour format with AM or PM
	      suffix. The default value is <em>nil</em>.</d>
      </it>

      <it>
	      <k/>
	      <f>display-time-day-and-date</f>
	      <d>If not set to <em>nil</em>, Emacs displays time with the
	      current day of the week, current month, and current day of the
	      month, instead of just the hour and minute. The default value
	      is <em>nil</em>.</d>
      </it>

      <it>
	      <k/>
	      <f>fill-column</f>
	      <d>Contains the number for the column on each line where text
	      begins to be filled to the next line. The default value is
	      70.</d>
      </it>

      <it>
	      <k/>
	      <f>initial-major-mode</f>
	      <d>Specifies the major mode to use for the <em>*scratch*</em>
	      buffer on startup. The default value is
	      <em>lisp-interaction-mode</em>.</d>
      </it>

      <it>
	      <k/>
	      <f>inverse-video</f>
	      <d>If not set to <em>nil</em>, Emacs inverts the display
	      colors, if possible. The default value is <em>nil</em>.</d>
      </it>

      <it>
	      <k/>
	      <f>kill-ring</f>
	      <d>Contains the contents of the Emacs kill ring.</d>
      </it>

      <it>
	      <k/>
	      <f>kill-ring-max</f>
	      <d>Sets the number of allowable entries in the Emacs kill
	      ring. The default value is 60.</d>
      </it>

      <it>
	      <k/>
	      <f>kill-whole-line</f>
	      <d>If not set to <em>nil</em>, the kill-line function (bound
	      to <kbd>C-k</kbd>) kills the current line and its trailing
	      newline character, if the function is executed at the very
	      beginning of the line. The default value is <em>nil</em>.</d>
      </it>

      <it>
	      <k/>
	      <f>make-backup-files</f>
	      <d>If not set to <em>nil</em>, Emacs saves a backup of a
	      buffer before any changes are made to a file of the same name
	      but with a tilde character (<kbd>~</kbd>) appended to the
	      end.</d>
      </it>

      <it>
	      <k/>
	      <f>mark-ring</f>
	      <d>Contains the contents of the current mark ring of the
	      buffer.</d>
      </it>

      <it>
	      <k/>
	      <f>mark-ring-max</f>
	      <d>Contains the number of allowable entries in the mark
	      ring. The default value is 16.</d>
      </it>

      <it>
	      <k/>
	      <f>mouse-avoidance-mode</f>
	      <d>Contains a value describing the type of mouse-avoidance
	      mode. The default value is <em>nil</em>.</d>
      </it>

      <it>
	      <k/>
	      <f>next-line-add-newline</f>
	      <d>If not set to <em>nil</em>, Emacs adds a new line whenever
	      the down arrow is pressed at the end of the buffer. The
	      default value is <em>nil</em> (in more recent versions of
	      Emacs).</d>
      </it>

      <it>
	      <k/>
	      <f>scroll-bar-mode</f>
	      <d>Contains the value for the side of the Emacs frame on which
	      to place the scroll bar: right or left. If set to
	      <em>nil</em>, the scroll bar is turned off.  The default value
	      is left.</d>
      </it>

      <it>
	      <k/>
	      <f>scroll-step</f>
	      <d>Contains the number of lines to move through the buffer
	      lines with the <em>scroll-down</em> and <em>scroll-up</em>
	      functions (which are bound to the <kbd>PgDn</kbd> and
	      <kbd>PgUp</kbd> keys by default). If set to 0, Emacs centers
	      point in the middle of the window when scrolling.</d>
      </it>

      <it>
	      <k/>
	      <f>show-trailing-whitespace</f>
	      <d>If not set to <em>nil</em>, Emacs makes any whitespace at
	      the end of lines in the current buffer visible. The default
	      value is <em>nil</em>.</d>
      </it>

      <it>
	      <k/>
	      <f>visible-bell</f>
	      <d>If not set to <em>nil</em>, Emacs makes the frame blink
	      instead of ringing the audible system bell. The default value
	      is <em>nil</em>.</d>
      </it>

      <it>
	      <k/>
	      <f>x-cut-buffer-max</f>
	      <d>Sets the maximum number of characters from the kill ring
	      that are also stored in the X Window System cut buffer. The
	      default value is 20000.</d>
      </it>
    </chapter>

    <chapter title="Interface functions">
      <it>
	      <k/>
	      <f>column-number-mode</f>
	      <d>Toggle the display, in the mode line, of the current column
	      the cursor is at, preceded by a <kbd>C</kbd>. The default
	      value is <em>nil</em>.</d>
      </it>

      <it>
	      <k/>
	      <f>display-time</f>
	      <d>Toggle the display of the current time in the mode
	      line. The default value is <em>nil</em>.</d>
      </it>

      <it>
	      <k/>
	      <f>font-lock-mode</f>
	      <d>If not set to <em>nil</em>, Emacs turns on the Font Lock
	      mode automatically for the current buffer. The default value
	      is <em>nil</em>.</d>
      </it>

      <it>
	      <k/>
	      <f>global-font-lock-mode</f>
	      <d>If not set to <em>nil</em>, Emacs turns on the Font Lock
	      mode automatically for all buffers. The default value is
	      <em>nil</em>.</d>
      </it>

      <it>
	      <k/>
	      <f>line-number-mode</f>
	      <d>Toggle the display, in the mode line, of the current line
	      the cursor is at, preceded by an <kbd>L</kbd>. The default
	      value is <em>t</em>.</d>
      </it>

      <it>
        <k/>
        <f>show-paren-mode</f>
        <d>Allows one to see matching pairs of parentheses and other
        characters. When point is on one of the paired characters, the
        other is highlighted.</d>
      </it>

      <it>
	      <k/>
	      <f>menu-bar-mode</f>
	      <d>Toggle the display of the Emacs menu bar. The default value
	      is <em>t</em>.</d>
      </it>

      <it>
	      <k/>
	      <f>sunrise-sunset</f>
	      <d>Display the time of today's sunrise and sunset for the
	      current geographic location. If preceded with the
	      <kbd>universal-argument</kbd>, this function prompts for a
	      specific day.</d>
      </it>

      <it>
	      <k/>
	      <f>tool-bar-mode</f>
	      <d>Toggle the display of the Emacs toolbar. The default value
	      is <em>t</em>.</d>
      </it>
    </chapter>

    <chapter title="Customize functions">
      <it>
        <k/>
        <f>customize-changed-options &lt;Enterversion&gt;</f>
        <d>Open a new customization buffer for all faces, options, or
        groups that have been changed since the version of Emacs given
        by version.</d>
      </it>

      <it>
        <k/>
        <f>customize-customized</f>
        <d>Open a new customization buffer for all options and faces
        that have already been customized but haven't been saved to
        disk.</d>
      </it>

      <it>
        <k/>
        <f>customize-face &lt;Enter&gt; &lt;regexp&gt;</f>
        <d>Open a new customization buffer for all the face, option,
        or groups relevant to the regular expression given by
        <kbd>&lt;regexp&gt;</kbd>.</d>
      </it>

      <it>
        <k/>
        <f>customize-face &lt;Enter&gt; &lt;face&gt;</f>
        <d>Open a new customization buffer for the face name given by
        <kbd>&lt;face&gt;</kbd>.</d>
      </it>

      <it>
        <k/>
        <f>customize-group &lt;Enter&gt; &lt;group&gt;</f>
        <d>Open a new customization buffer for the group name given by
        <kbd>&lt;group&gt;</kbd>.</d>
      </it>

      <it>
        <k/>
        <f>customize-option &lt;Enter&gt; &lt;option&gt;</f>
        <d>Open a new customization buffer for the option name given
        by <kbd>&lt;option&gt;</kbd>.</d>
      </it>

      <it>
        <k/>
        <f>customize-saved</f>
        <d>Open a new customization buffer for all faces and options
        that you've changed with the Customize function.</d>
      </it>
    </chapter>

    <chapter title="Help commands">
      <it>
        <k>Tab</k>
        <f/>
        <d>This command performs command completion if given as part
        of a command, showing all possible input values for the given
        command.</d>
      </it>

      <it>
        <k>&lt;command prefix&gt; or &lt;keystroke&gt; C-h</k>
        <f/>
        <d>This command describes all the possible commands and
        functions available for the given <kbd>&lt;command
        prefix&gt;</kbd> or <kbd>&lt;keystroke&gt;</kbd>.</d>
      </it>

      <it>
        <k>C-h c &lt;keystroke&gt;</k>
        <f>describe-key-briefly</f>
        <d>This command reports in the minibuffer the name of function
        that <kbd>&lt;keystroke&gt;</kbd>is bound to.</d>
      </it>

      <it>
        <k>C-h k &lt;keystroke&gt;</k>
        <f>describe-key</f>
        <d>This command opens a new help-buffer window that describes
        the function that <kbd>&lt;keystroke&gt;</kbd> is bound
        to.</d>
      </it>

      <it>
        <k>C-h l</k>
        <f>view-lossage</f>
        <d>This command opens a new buffer and displays the last 100
        characters typed.</d>
      </it>

      <it>
        <k/>
        <f>open-dribble-file</f>
        <d>This command opens a specified file and dribbles a copy of
        all keyboard input to that file.</d>
      </it>

      <it>
        <k/>
        <f>apropos</f>
        <d>This command gives a list of apropos commands and variables
        to a given <kbd>regexp</kbd>.</d>
      </it>

      <it>
        <k>C-h a &lt;regexp&gt;</k>
        <f>command-apropos</f>
        <d>This command gives a list of apropos commands to
        regexp.</d>
      </it>

      <it>
        <k>C-h b</k>
        <f>describe-bindings</f>
        <d>This command describes all the valid key bindings for the
        current major mode in a new help buffer window.</d>
      </it>

      <it>
        <k>C-h f &lt;function&gt;</k>
        <f>describe-function</f>
        <d>This command describes the purpose of
        <kbd>&lt;function&gt;</kbd> in a new help buffer window.</d>
      </it>

      <it>
        <k>C-h v &lt;variable&gt;</k>
        <f>describe-variable</f>
        <d>This command describes the purpose of
        <kbd>&lt;variable&gt;</kbd> in a new help buffer window.</d>
      </it>

      <it>
        <k>C-h w &lt;function&gt;</k>
        <f>where-is</f>
        <d>This command describes which keyboard binding (if any) a
        particular <kbd>&lt;function&gt;</kbd> is bound to.</d>
      </it>

      <it>
        <k>C-h s | F1 s</k>
        <f>describe-syntax</f>
        <d>Display the <em>Emacs Syntax Table</em> for the current
        mode (useful for the matching parentheses commands).</d>
      </it>
    </chapter>

    <chapter title="INFO commands">
      <it>
        <k>H</k>
        <f>Info-help</f>
        <d>This command opens a hands-on <kbd>Info</kbd> tutorial in a
        new buffer.</d>
      </it>

      <it>
        <k>Q</k>
        <f>Info-exit</f>
        <d>This command moves to the last buffer you visited, putting
        the <kbd>*info*</kbd> buffer in the end of the buffer
        list.</d>
      </it>

      <it>
        <k>&lt;Enter&gt;</k>
        <f/>
        <d>This command follows the cross reference at or near
        point.</d>
      </it>

      <it>
        <k>N</k>
        <f>Info-next</f>
        <d>This command moves to the current node's <kbd>Next</kbd>
        node.</d>
      </it>

      <it>
        <k>P</k>
        <f>Info-prev</f>
        <d>This command moves to the current node's
        <kbd>Previous</kbd> node.</d>
      </it>

      <it>
        <k>U</k>
        <f>Info-up</f>
        <d>This command moves to the current node's <kbd>Up</kbd>
        node.</d>
      </it>

      <it>
        <k>D</k>
        <f>Info-directory</f>
        <d>This command moves to the <kbd>Directory</kbd> node.</d>
      </it>

      <it>
        <k>L</k>
        <f>Info-last</f>
        <d>This command moves to the last node you visited.</d>
      </it>

      <it>
        <k>T</k>
        <f>Info-top-node</f>
        <d>This command moves to the <kbd>Top</kbd> node of the
        current document.</d>
      </it>

      <it>
        <k>&gt;</k>
        <f>Info-final-node</f>
        <d>This command moves to the final node pointed to in the
        current document.</d>
      </it>

      <it>
        <k>&lt;Spacebar&gt;</k>
        <f>Info-scroll-up</f>
        <d>This command moves forward in the current node by a single
        screen; if at the end of the node, then move to the Next
        node.</d>
      </it>

      <it>
        <k>&lt;Backspace&gt;</k>
        <f>Info-scroll-down</f>
        <d>This command moves backward in the current node by a single
        screen; if at the beginning of the node, then move to the
        Previous node.</d>
      </it>

      <it>
        <k>B</k>
        <f>beginning-of-buffer</f>
        <d>This command goes to the beginning of the current node.</d>
      </it>

      <it>
        <k>S</k>
        <f>Info-search</f>
        <d>This command searches forward in the current
        <kbd>Info</kbd> document for a given <kbd>regexp</kbd>.</d>
      </it>

      <it>
        <k>Tab</k>
        <f>Info-next-reference</f>
        <d>This command moves the cursor forward to the first cross
        reference.</d>
      </it>

      <it>
        <k>M-Tab</k>
        <f>Info-prev-reference</f>
        <d>This command moves the cursor backward to the last cross
        reference.</d>
      </it>
    </chapter>

    <chapter title="Documentation files">
      <it>
        <k>C-h C-d</k>
        <f>describe-distribution</f>
        <d><kbd>DISTRIB</kbd> Information on obtaining a copy of the
        latest distribution of the Emacs software</d>
      </it>

      <it>
        <k>C-h F</k>
        <f>view-emacs-faq</f>
        <d><kbd>/usr/share/info/</kbd>;
        <kbd>emacs-mainversion/efaq.gz</kbd> Emacs FAQ</d>
      </it>

      <it>
        <k>C-h C-c</k>
        <f>describe-copying</f>
        <d><kbd>COPYING</kbd> GNU General Public License (GNU GPL)</d>
      </it>

      <it>
        <k>C-h C-w</k>
        <f>describe-no-warranty</f>
        <d><kbd>COPYING</kbd> Section "NO WARRANTY" of the GNU General
        Public License (GNU GPL)</d>
      </it>

      <it>
        <k>C-h n</k>
        <f>view-emacs-news</f>
        <d><kbd>NEWS</kbd> News concerning the latest changes in the
        current version of Emacs</d>
      </it>

      <it>
        <k>C-h P</k>
        <f>view-emacs-problems</f>
        <d><kbd>PROBLEMS</kbd> Emacs problems file</d>
      </it>

      <it>
        <k>C-h C-p</k>
        <f>describe-project</f>
        <d><kbd>THE-GNU-PROJECT</kbd> Essay by Richard Stallman
        concerning the founding of the GNU Project</d>
      </it>

      <it>
        <k>C-h t</k>
        <f>help-with-tutorial</f>
        <d><kbd>TUTORIAL</kbd> Hands-on tutorial for learning the
        basics of Emacs</d>
      </it>
    </chapter>

    <chapter title="Things you should never know about">
      <it>
        <k/>
        <f>hanoi</f>
        <d>The hanoi tower, with a default of 3 discs; <kbd>M-x
        hanoi-unix</kbd> and <kbd>M-x hanoi-unix-64</kbd> uses the
        unix timestamp, making a move each second in line with the
        clock, and with the latter pretending it uses a 64-bit
        clock.</d>
      </it>

      <it>
        <k/>
        <f>5x5</f>
        <d>You are given a 5x5 grid with a central cross already
        filled-in; your goal is to fill all the cells by toggling them
        on and off in the right order to win. With an optional digit
        argument you can change the size of the grid.</d>
      </it>

      <it>
        <k/>
        <f>animate-birthday-present</f>
        <d>A fancy birthday present animation.</d>
      </it>

      <it>
        <k/>
        <f>butterfly</f>
        <d>The animate package is also used by <kbd>M-x
        butterfly</kbd> command, a command added to Emacs as an homage
        to the XKCD strip at <kbd>www.xkcd.com/378/</kbd></d>
      </it>

      <it>
        <k/>
        <f>blackbox</f>
        <d>The object of the game is to find four hidden balls by
        shooting rays into the black box. There are four
        possibilities: 1) the ray will pass thru the box undisturbed,
        2) it will hit a ball and be absorbed, 3) it will be deflected
        and exit the box, or 4) be deflected immediately, not even
        being allowed entry into the box.</d>
      </it>

      <it>
        <k/>
        <f>bubbles</f>
        <d>You must clear out as many “bubbles” as you can in as few
        moves as possible. When you remove bubbles the other bubbles
        drop and stick together. You can configure the difficulty of
        the game by calling <kbd>M-x
        bubbles-set-game-&lt;difficulty&gt;</kbd> where
        &lt;difficulty&gt; is one of: <kbd>easy</kbd>,
        <kbd>medium</kbd>, <kbd>difficult</kbd>, <kbd>hard</kbd>, or
        <kbd>userdefined</kbd>. Furthermore, you can alter the
        graphics, grid size and colors using Customize: <kbd>M-x
        customize-group bubbles</kbd>.</d>
      </it>

      <it>
        <k/>
        <f>decipher</f>
        <d>It’s a (very complex) package to help you break simple
        substitution ciphers (like cryptogram puzzles) using a helpful
        user interface.</d>
      </it>

      <it>
        <k/>
        <f>dissociated-press</f>
        <d>It's a semi-randomizing algorithm that takes your buffer,
        runs it through a blender, and displays the result.</d>
      </it>

      <it>
        <k/>
        <f>doctor</f>
        <d>Based on the original <em>ELIZA</em>, the “Doctor” tries to
        psychoanalyze what you say and attempts to repeat the question
        back to you. It simulates a Rogerian psychotherapist and uses
        rules, dictated into a script, to respond with non-directional
        questions to user inputs.</d>
      </it>

      <it>
        <k/>
        <f>dunnet</f>
        <d>Emacs’s very own Zork-like text adventure game.</d>
      </it>

      <it>
        <k/>
        <f>gomoku</f>
        <d>You have to connect 5 squares, tic-tac-toe style. You can
        customize the group <kbd>gomoku</kbd> to adjust the size of
        the grid.</d>
      </it>

      <it>
        <k/>
        <f>life</f>
        <d>Conway’s Game of Life is a famous example of cellular
        automata. The Emacs version comes with a handful of starting
        patterns that you can (programmatically with elisp) alter by
        adjusting the <kbd>life-patterns</kbd> variable.</d>
      </it>

      <it>
        <k/>
        <f>pong | snake | tetris</f>
        <d>These classic games are all implemented using the Emacs
        package gamegrid, a generic framework for building grid-based
        games like Tetris and Snake.</d>
      </it>

      <it>
        <k/>
        <f>solitaire</f>
        <d>It is a peg-based game where you have to end up with just
        one stone on the board, by taking a stone (the <kbd>o</kbd>)
        and “jumping” over an adjacent stone into the hole (the
        <kbd>.</kbd>), removing the stone you jumped over in the
        process. Rinse and repeat until the board is empty. There is a
        handy solver built in called <kbd>M-x solitaire-solve</kbd> if
        you get stuck.</d>
      </it>

      <it>
        <k/>
        <f>zone</f>
        <d>A series of screensavers. Type <kbd>M-x zone</kbd> and
        watch what happens to your screen! You can configure a
        screensaver idle time by running <kbd>M-x zone-when-idle</kbd>
        (or calling it from elisp) with an idle time in seconds. You
        can turn it off with <kbd>M-x zone-leave-me-alone</kbd>.</d>
      </it>

      <it>
        <k/>
        <f>mpuz</f>
        <d>A multiplication puzzle where you have to replace the
        letters with numbers and ensure the numbers add (multiply?)
        up. You can run <kbd>M-x mpuz-show-solution</kbd> to solve the
        puzzle if you get stuck.</d>
      </it>

      <it>
        <k/>
        <f>morse-region</f>
        <d>Translates a region into morse code. Undo
        (<kbd>C-S-_</kbd>) or <kbd>M-x unmorse-region</kbd> to
        unmorse.</d>
      </it>
    </chapter>

  </body>

  <footer>
    <line>Copyright (C) 2017 Pete Za Sayari</line>
    <line>&lt;petezasayari@gmail.com&gt;</line>
    <line/>
    <line>Released under the terms of the GNU General Public
    License</line>
    <line>version 3 or later.</line>
    <line/>
    <line>For more Emacs documentation see the Emacs distribution or
    http://www.gnu.org/software/emacs</line>
    <line>For the XML-XSLT source for this cheat sheet see
    https://github.com/PeteZaSayari/emacs-cs</line>
  </footer>

</cheatsheet>

<!-- Dictionary

  <it>  =   <item>
  <k>   =   <key>
  <f>   =   <function>
  <d>   =   <description>

-->
